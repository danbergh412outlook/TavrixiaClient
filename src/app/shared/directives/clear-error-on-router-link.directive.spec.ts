import { ClearErrorOnRouterLinkDirective } from './clear-error-on-router-link.directive';

describe('ClearErrorOnRouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearErrorOnRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
